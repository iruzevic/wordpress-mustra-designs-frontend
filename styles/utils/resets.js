import {css} from 'emotion';

const listReset = css`
  margin: 0;
  padding: 0;
  list-style: none;
`;

const headingReset = css`
  margin: 0;
  padding: 0;
`;

export const resets = {
  list: listReset,
  heading: headingReset,
  paragraph: headingReset,
};

